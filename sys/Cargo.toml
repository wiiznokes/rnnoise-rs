[package]
name = "rnnoise2-sys"
version = "0.1.0"
edition.workspace = true
license.workspace = true
categories = ["api-bindings"]
description = "low level bindings for the rnnoise library"
keywords.workspace = true

[dependencies]

[build-dependencies]
bindgen = "0.72"
cc = "1"
flate2 = "1"
reqwest = {version = "0.13", features = ["blocking"] }
tar = "0.4"
anyhow = "1"

[dev-dependencies]
